using System;

namespace task3
{
    class Program
    {
        static void Main(string[] args)
        {   //task: Find the description of the System.Console.WriteLine() method and its different possible parameters in the MSDN Library.
            Console.WriteLine("Writes the specified data, followed by the current line terminator, to the standard output stream.");
            Console.ReadLine();
        }
    }
}
