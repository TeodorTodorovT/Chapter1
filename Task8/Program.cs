using System;

namespace Task8
{
    class Program
    {
        static void Main(string[] args)
        {   //task: Write a program that prints on the console the current date and time.
            DateTime now = DateTime.Now;
            Console.WriteLine(now);
            Console.ReadLine();
        }
    }
}
