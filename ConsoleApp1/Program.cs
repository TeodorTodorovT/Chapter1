using System;

namespace ConsoleApp1
{
    class Program
    {
        static void Main(string[] args)
        {   //task:  Find the description of the System.Console class in the standard .NET API documentation (MSDN Library).
            Console.WriteLine("Represents the standard input, output, and error streams for console applications. This class cannot be inherited.");
            Console.ReadLine();
        }
    }
}
